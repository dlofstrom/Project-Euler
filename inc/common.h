#ifndef COMMON_H
#define COMMON_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>


//Prime functions
uint8_t generate_primes(uint64_t n);
uint8_t is_prime(uint64_t n);
uint64_t get_prime(uint32_t i);

#endif
